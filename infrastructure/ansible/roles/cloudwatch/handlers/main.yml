# ============================================================================
# CLOUDWATCH ROLE - HANDLERS
# ============================================================================

---
- name: Restart CloudWatch Agent
  ansible.builtin.systemd:
    name: amazon-cloudwatch-agent
    state: restarted
