# ============================================================================
# APP ROLE - DEFAULT VARIABLES
# ============================================================================

---
# Application name
app_name: portfolio

# Application port (container listens on port 80, mapped to this host port)
app_port: 3000

# AWS region
aws_region: "us-east-1"

# ECR repository URL (should be set in inventory)
ecr_repository_url: ""
